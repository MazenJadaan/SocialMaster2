import 'package:flutter/material.dart';

class SeePage extends StatelessWidget {
  const SeePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("See"),),
    );
  }
}
